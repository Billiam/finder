<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:points, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:points, :edit, :id => @point.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :point, url(:points, :update, :id => @point.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'points/form', :locals => { :f => f } %>
  <% end %>
</div>
